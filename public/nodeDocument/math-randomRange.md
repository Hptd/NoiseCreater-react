# 随机范围（Random Range）

- 分类：数学
- 英文标识：math:randomRange

## 功能介绍
由种子生成范围内伪随机值。

## 如何使用
把图像或数值连到输入端口；未连线的端口使用面板中的常量。

## 输入
- **seed**：seed 输入；未连线时使用面板常量
- **min**：min 输入；未连线时使用面板常量
- **max**：max 输入；未连线时使用面板常量

## 输出
- **out**：图像输出（RGBA）

## 参数
- 随机种子（seed）：默认 0
- 最小值（min）：默认 0
- 最大值（max）：默认 1
