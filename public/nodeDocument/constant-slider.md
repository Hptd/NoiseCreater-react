# 滑块（Slider）

- 分类：输入
- 英文标识：constant:slider

## 功能介绍
带范围约束的数值常量，用于手动调节。

## 如何使用
拖入画布后设置数值 / 颜色；也可作为数据节点连接到节点的参数端口，用于外部驱动参数。

## 输入
无图像输入。

## 输出
- **out**：图像输出（RGBA）

## 参数
- 值（value）：默认 0.5
- 最小值（min）：默认 0
- 最大值（max）：默认 1
